{
  "extends": [
    "react-app",
    "react-app/jest"
  ],
  "rules": {
    "no-console": "warn",
    "prefer-const": "error",
    "quotes": ["warn", "single"],
    "indent": ["warn", 2],
    "max-len": ["warn", {"code": 120}],
    "semi": ["warn", "always"]
  }
}